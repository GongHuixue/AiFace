package android.com.aiface.settings;

public class AiFaceEnum {
    public enum CollectMode {
        COLLECT_NONE,
        COLLECT_MEETING,
        COLLECT_ATTENDANCE,
        COLLECT_HOME,
        COLLECT_GATE
    }

    public enum DetectMode {
        DETECT_NONE,
        DETECT_MEETING,
        DETECT_ATTENDANCE,
        DETECT_HOME,
        DETECT_GATE
    }
}
